# SatQuery AI — Backend Dependencies
# GDAL/Rasterio install path: Path B (rasterio manylinux wheels — no bare GDAL pip install)
# Command: pip install -r requirements.txt
# For Path A (conda): conda install -c conda-forge gdal=3.8 rasterio=1.3.* rioxarray

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
pydantic==2.10.3
pydantic-settings==2.6.1

# Database (in-process SQLite — jobs survive backend reload)
sqlalchemy==2.0.36

# Geospatial image processing (Path B: manylinux wheels, GDAL vendored)
rasterio==1.3.9
rioxarray==0.17.0
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==11.0.0

# Deep learning
torch==2.4.1
torchvision==0.19.1
transformers==4.47.1
timm==1.0.12

# Report generation
reportlab==4.2.5

# Utilities
python-dotenv==1.0.1
httpx==0.27.2
aiofiles==24.1.0
